---
const {
  class: className = "",
  ...rest
} = Astro.props;
---

<div
  class={`bg-white rounded-lg border border-gray-200 shadow-sm p-5 hover:shadow-md transition ${className}`}
  {...rest}
>
  <slot />
</div>